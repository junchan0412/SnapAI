import Foundation

enum ResultDiagnosticsCommand {
    static let briefTitle = "复制精简请求诊断"
    static let briefCompactTitle = "复制精简"
    static let briefSubtitle = "模型选择、fallback、恢复建议和隐私摘要,不含错误详情"
    static let title = "复制完整请求诊断"
    static let compactTitle = "复制完整"
    static let subtitle = "模型选择、fallback、恢复建议、隐私和请求尝试详情"
    static let systemImage = "point.3.connected.trianglepath.dotted"
    static let briefKeywords = "result diagnostics summary brief share route fallback model privacy redaction history recovery suggestion unavailable api key copy 请求 路由 诊断 摘要 精简 模型 隐私 脱敏 历史 恢复 修复 建议 无可用 API Key 复制"
    static let keywords = "result diagnostics full route fallback model privacy redaction history error attempt recovery suggestion unavailable api key copy 请求 路由 诊断 完整 模型 隐私 脱敏 历史 错误 尝试 恢复 修复 建议 无可用 API Key 复制"
}
